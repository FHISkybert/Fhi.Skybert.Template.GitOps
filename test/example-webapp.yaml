apiVersion: skybert.fhi.no/v1
kind: WebApp
metadata:
  name: example-app
  namespace: tn-<TENANT>
spec:
  container:
    image:
      repository: paulbouwer/hello-kubernetes
      tag: "1.10"

  ingress:
    enabled: true
    hostname: <TENANT>.skytest.fhi.no
